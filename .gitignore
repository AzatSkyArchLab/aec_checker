node_modules/
dist/
*.local
.DS_Store

# WASM копируется из node_modules скриптом copy-wasm (postinstall) — не коммитим
public/web-ifc.wasm

# ПРИВАТНЫЙ каталог проверок ЦИМ АГР (алгоритмы + НПА) — генерируется из xlsx,
# в публичный репозиторий не попадает (только движок). См. scripts/gen-catalog.py
public/checks-catalog.json
public/checks-probemap.json

# ПРИВАТНО: рабочие материалы по проверкам (DWG-чеклисты, build-скрипты, исходные PDF) —
# валидационный моат, не для публичного репо
docs/

# Логи
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Редакторы
.vscode/*
!.vscode/extensions.json
.idea/
public/*.fbx
